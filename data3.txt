// -------------------------------
// STATIC LISTS + RANDOM PICK (no structure, no split)
// -------------------------------

// Allowed address types from your API error
def addressTypes = ['HOME', 'OFFICE', 'BILLING']

def countryCodes = ['MX']

def stateCds = ['CMX', 'JAL', 'NLE', 'PUE']
def stateNames = ['Ciudad de Mexico', 'Jalisco', 'Nuevo León', 'Puebla']

def townNames = [
    'Benito Juárez',
    'Guadalajara',
    'Monterrey',
    'Puebla'
]

def postalCodes = [
    '031000',
    '441000',
    '640000',
    '720000'
]

def neighbourhoodNames = [
    'Del Valle',
    'Centro',
    'Obispado',
    'Cholula'
]

def addressNames = [
    'Insurgentes Sur',
    'Av. Vallarta',
    'Constitución',
    'Blvd. 5 de Mayo'
]

def streetNbs = ['12', '45', '89', '123']
def complements = ['Depto. 101', 'Depto. 202', 'Casa 4', 'Interior 3']

// ---- Random pick per field ----
vars.put('addressType', addressTypes[rand.nextInt(addressTypes.size())])
vars.put('countryCode', countryCodes[rand.nextInt(countryCodes.size())])
vars.put('stateCd', stateCds[rand.nextInt(stateCds.size())])
vars.put('stateName', stateNames[rand.nextInt(stateNames.size())])
vars.put('townName', townNames[rand.nextInt(townNames.size())])
vars.put('postalCode', postalCodes[rand.nextInt(postalCodes.size())])
vars.put('neighbourhoodName', neighbourhoodNames[rand.nextInt(neighbourhoodNames.size())])
vars.put('addressName', addressNames[rand.nextInt(addressNames.size())])
vars.put('streetNb', streetNbs[rand.nextInt(streetNbs.size())])
vars.put('complementAddressName', complements[rand.nextInt(complements.size())])

log.info("STATIC RANDOM ADDRESS SET")
