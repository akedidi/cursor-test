Hi EIS team,

API Gateway confirmed that no failed token requests are visible on their side, suggesting that the request may not be forwarded beyond the mTLS Gateway.

Could you please investigate the mTLS Gateway logs to confirm whether the request is received, whether it is successfully forwarded to APIGW, and which response or timeout is returned to EIS? Please also check for any connection drops, routing, DNS/IP resolution or upstream connectivity issues between the mTLS Gateway and APIGW.

The objective is to identify whether the timeout occurs while forwarding the request from the mTLS Gateway to APIGW.
